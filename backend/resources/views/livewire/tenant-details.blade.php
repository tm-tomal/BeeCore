<div class="space-y-6">
    <header class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><a href="{{ route('tenants') }}" class="text-sm font-semibold text-teal-300">← Tenant portfolio</a><h1 class="mt-3 text-2xl font-black text-white sm:text-3xl">{{ $tenant->name }}</h1><p class="mt-2 text-sm text-slate-500">{{ $tenant->slug }} · {{ ucfirst($tenant->status) }} · {{ $tenant->currency }} · {{ $tenant->timezone }}</p></div><a href="{{ route('tenants') }}" class="bc-secondary">Edit tenant</a></header>
    @if(session()->has('message'))<div class="border border-emerald-400/20 bg-emerald-400/10 p-3 text-sm text-emerald-300" style="border-radius:6px">{{ session('message') }}</div>@endif
    <section class="grid gap-3 sm:grid-cols-3">
        <div class="bc-panel p-5"><p class="text-xs font-bold uppercase text-slate-500">Customers</p><p class="mt-3 text-3xl font-black text-white">{{ $counts['customers'] }}</p>@if($subscription?->plan?->customer_limit)<p class="mt-2 text-xs {{ $counts['customers'] > $subscription->plan->customer_limit ? 'text-rose-300' : 'text-slate-600' }}">Limit {{ $subscription->plan->customer_limit }}</p>@endif</div>
        <div class="bc-panel p-5"><p class="text-xs font-bold uppercase text-slate-500">Active staff</p><p class="mt-3 text-3xl font-black text-white">{{ $counts['staff'] }}</p>@if($subscription?->plan?->staff_limit)<p class="mt-2 text-xs {{ $counts['staff'] > $subscription->plan->staff_limit ? 'text-rose-300' : 'text-slate-600' }}">Limit {{ $subscription->plan->staff_limit }}</p>@endif</div>
        <div class="bc-panel p-5"><p class="text-xs font-bold uppercase text-slate-500">Resellers</p><p class="mt-3 text-3xl font-black text-white">{{ $counts['resellers'] }}</p>@if($subscription?->plan?->reseller_limit)<p class="mt-2 text-xs {{ $counts['resellers'] > $subscription->plan->reseller_limit ? 'text-rose-300' : 'text-slate-600' }}">Limit {{ $subscription->plan->reseller_limit }}</p>@endif</div>
    </section>
    <section class="grid gap-5 xl:grid-cols-[1fr_0.8fr]"><div class="bc-panel p-5 sm:p-6"><h2 class="font-bold text-white">SaaS subscription</h2>@if($plans->isEmpty())<p class="mt-5 text-sm text-amber-300">Create an active SaaS plan before assigning a subscription.</p>@else<form wire:submit="saveSubscription" class="mt-5 space-y-4"><div class="grid gap-4 sm:grid-cols-2"><div><label class="bc-label" for="sub-plan">Plan</label><select id="sub-plan" wire:model="planId" class="bc-field"><option value="">Select plan</option>@foreach($plans as $plan)<option value="{{ $plan->id }}">{{ $plan->name }}</option>@endforeach</select>@error('planId')<p class="text-xs text-rose-300">{{ $message }}</p>@enderror</div><div><label class="bc-label" for="sub-cycle">Billing cycle</label><select id="sub-cycle" wire:model="billingCycle" class="bc-field"><option value="monthly">Monthly</option><option value="yearly">Yearly</option></select></div><div><label class="bc-label" for="sub-status">Status</label><select id="sub-status" wire:model.live="subscriptionStatus" class="bc-field"><option value="trialing">Trialing</option><option value="active">Active</option><option value="paused">Paused</option><option value="past_due">Past due</option><option value="suspended">Suspended</option><option value="cancelled">Cancelled</option></select></div><div><label class="bc-label" for="sub-starts">Starts at</label><input id="sub-starts" wire:model="startsAt" type="date" class="bc-field"></div>@if($subscriptionStatus === 'trialing')<div><label class="bc-label" for="sub-trial-ends">Trial ends at</label><input id="sub-trial-ends" wire:model="trialEndsAt" type="date" class="bc-field"></div>@endif</div><label class="inline-flex items-center gap-3 text-sm text-slate-300"><input wire:model="autoRenew" type="checkbox">Auto renew subscription</label><div><button type="submit" class="bc-primary">Save subscription</button></div></form>@endif</div><div class="bc-panel p-5 sm:p-6"><h2 class="font-bold text-white">Current terms</h2>@if($subscription)<dl class="mt-5 space-y-4 text-sm"><div class="flex justify-between"><dt class="text-slate-500">Plan</dt><dd class="font-bold text-white">{{ $subscription->plan->name }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Status</dt><dd class="capitalize text-teal-300">{{ $subscription->status }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Price</dt><dd class="text-white">৳{{ number_format($subscription->price, 2) }} / {{ $subscription->billing_cycle }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Period ends</dt><dd class="text-white">{{ $subscription->current_period_ends_at?->format('d M Y') }}</dd></div><div class="flex justify-between"><dt class="text-slate-500">Grace ends</dt><dd class="text-white">{{ $subscription->grace_ends_at?->format('d M Y') }}</dd></div></dl><h3 class="mt-7 text-sm font-bold text-white">History</h3><div class="mt-3 space-y-3">@foreach($subscription->events->sortByDesc('created_at')->take(6) as $event)<div class="border-l-2 border-teal-400/30 pl-3"><p class="text-xs font-semibold text-slate-300">{{ str_replace('.', ' ', $event->event) }}</p><p class="mt-1 text-xs text-slate-600">{{ $event->from_status ?? 'new' }} → {{ $event->to_status }} · {{ $event->created_at->diffForHumans() }}</p></div>@endforeach</div>@else<p class="mt-5 text-sm text-slate-600">No SaaS subscription assigned.</p>@endif</div></section>
    <section class="bc-panel p-5 sm:p-6">
        <h2 class="font-bold text-white">SaaS invoices</h2>
        <div class="bc-table-wrap mt-5"><table class="bc-table"><thead><tr><th>Invoice</th><th>Period</th><th>Amount</th><th>Due</th><th>Status</th><th class="text-right">Action</th></tr></thead><tbody>
            @forelse($invoices as $invoice)
                <tr>
                    <td><code class="text-teal-300">{{ $invoice->invoice_number }}</code></td>
                    <td>{{ $invoice->period_start->format('d M Y') }} – {{ $invoice->period_end->format('d M Y') }}</td>
                    <td>৳{{ number_format($invoice->amount, 2) }}</td>
                    <td>{{ $invoice->due_date->format('d M Y') }}</td>
                    <td><span class="capitalize font-semibold {{ match($invoice->status) { 'paid' => 'text-emerald-300', 'overdue' => 'text-rose-300', default => 'text-amber-300' } }}">{{ $invoice->status }}</span></td>
                    <td class="text-right">@if(in_array($invoice->status, ['pending', 'overdue']))<button wire:click="markInvoicePaid({{ $invoice->id }})" wire:confirm="Mark this SaaS invoice as paid?" class="font-semibold text-teal-300">Mark paid</button>@endif</td>
                </tr>
            @empty
                <tr><td colspan="6" class="py-10 text-center text-slate-600">No SaaS invoices generated yet.</td></tr>
            @endforelse
        </tbody></table></div>
    </section>
</div>